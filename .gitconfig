[core]
    editor = nvim
    autocrlf = input
[alias]
    st = status
    aa = add .
    cam = commit -am
    rs = restore .
    lg = log --graph --decorate --oneline --abbrev-commit
    ri = rebase -i HEAD~10
